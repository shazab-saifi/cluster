# Project Agent Config

Skills are stored in `.agents/skills`.

Load relevant skill files depending on the task:

- Frontend tasks → `.agents/skills/frontend.md`

Always consult matching skill files before making changes.
